# Set in Vercel for production. Leave UNSET locally — the Vite proxy
# (vite.config.ts) handles dev by forwarding /api to Django on :8000.
#
# Example:
#   VITE_API_BASE_URL=https://playto-payout.onrender.com
VITE_API_BASE_URL=
